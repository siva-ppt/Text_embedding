# Reproduction Guide

1. Clone the repository.
2. Install requirements: `pip install -r requirements.txt`
3. Run benchmarks: `python articles/01-text-embeddings/run_benchmarks.py`
4. View results in `articles/01-text-embeddings/results/`.
